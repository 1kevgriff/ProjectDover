namespace ProjectDover
{
    public enum Command
    {
        UNKNOWN = -1,
        COMMAND_HANDLED,
        COMMAND_QUIT,
        COMMAND_NORTH,
        COMMAND_SOUTH,
        COMMAND_EAST,
        COMMAND_WEST,
        COMMAND_LOOK
    }
}
